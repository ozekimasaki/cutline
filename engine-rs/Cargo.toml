[package]
name = "cutline-engine"
version = "0.1.0"
edition = "2021"
description = "CutLine decide/cameras CLI. Jev is never asked CUT or which camera."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
